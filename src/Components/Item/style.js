import styled from 'styled-components';

export const ItemWrapper = styled.div`
  font-size: 22px;

  textarea {
    border: none;
    outline: none;
    resize: none;
    width: 100%;
    font-size: inherit;
  }
`;