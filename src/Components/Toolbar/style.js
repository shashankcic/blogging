import styled from 'styled-components';

export const ToolbarWrapper = styled.div`
  padding: 16px;
  button {
    width: 50px;
    height: 50px;
    border-radius: 15px;
    background: none;
    &:hover {
      cursor: pointer;
    }
  }
`;